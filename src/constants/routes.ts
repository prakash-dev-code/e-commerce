export enum AuthRoutes {
    SignIn = "/sign-in",
    SignUp = "/sign-up",
  }

  export enum AppRoutes {
    MyAccount = "/my-account",
    Orders = "/orders",
    Wishlist = "/wishlist",
    Rewards = "/rewards",
  }